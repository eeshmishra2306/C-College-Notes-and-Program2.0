#include <stdio.h>

int main() {
    int n, fact = 1, sum = 0;

    printf("Enter value of n: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        fact *= i;
        sum += fact;
    }

    printf("Sum = %d\n", sum);
    return 0;
}
